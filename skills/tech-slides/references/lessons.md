# 経験の蓄積（ユーザーからの指摘と、それをどう仕組みに落としたか）

このファイルはプラグインの「経験」。ユーザーから見た目・構成・進め方について指摘を受けたら、
**その場で直すだけでなく、同じことが二度と起きないように仕組み（テーマ・lint・チェックリスト・スキル本文）へ落とし、ここに 1 行残す。**
書式：`日付 | 指摘（要約） | 一般化した規則 | 落とした先`

作業の前に一読し、規則は既に守られているものとして扱う。仕組みに落とせなかった指摘は「未対応」と書いて残す。

## 見た目

- 2026-09-19 | タイトルとリード文が詰まって見える | 見出しの下は 26px、リードの下は 44px 空ける。見出しの直下に本文を密着させない | テーマ `h1` / `h1 + p` の margin
- 2026-09-19 | ベン図で右の円が左を完全に覆い、重なりが分からない | 重ね合わせる図形は半透明 + `mix-blend-mode: multiply` で、重なった領域が第 3 の色になるようにする。両方の輪郭を残す | テーマ `.venn`
- 2026-09-19 | 本文 + 図（`two.wide-right`）で左の箇条書きが 1 行ずつ折り返して読みにくい | 図は「大きく」より「文字が折り返さない幅」を優先する。2 分割の本文は 25px で 12〜14 字が 1 行に入る幅を確保する | テーマ `.two.wide-right` を 1 : 1.15 に、`.two ul` を 25px
- 2026-09-19 | 白背景のスクリーンショットを 2 枚並べると、アイボリーの地に溶けて境界が曖昧になる | 画像はそれ自体の背景色がスライドの地と近いことが多い。並べるときは面（`--surface`）の枠で囲って 1 枚ずつの輪郭を作る | テーマ `.gallery figure`
- 2026-09-18 | ターミナル風コードで文字列の色が背景に沈む | 暗色背景のハイライトは別配色を定義し、`section.terminal` の形でセレクタを書く（Marp のスコープ書き換え対策） | テーマ `.terminal`、`marp-notes.md`

- 2026-09-19 | Mermaid を SVG で貼ると Marp 側の書体で再計測されず、ノード内の文字が右端で切れた | Mermaid は SVG でなく PNG（`-s 2`、`-b transparent`）で貼る。書体・配色は `-c config.json` の `themeVariables` と `themeCSS` で指定し、既定の紫のラベル背景は `edgeLabelBackground` を地の色にする | `marp-notes.md` の Mermaid 節
- 2026-09-19 | 文字だけの枚が続いて疲れる。リード文が 2〜3 行に折り返して見栄えが悪い。表紙が地味 | 3 枚に 1 枚は実画面・図にする。リード文は 1 行（全角 36 字目安）に収め、lint W02 の 60 字より厳しく見る。表紙は `title bold`（強調色の濃い面）を既定候補にする | テーマ `section.title.bold`、`.map`；SKILL.md の量の目安
- 2026-09-19 | 補足文（.note）に「他に〜もある」を毎枚のように足していた。細かく書きすぎる | .note は出典 1 行まで。足したい情報は「1 枚にする／話者ノートへ／捨てる」の三択。あえて書かないを選ぶ | lint W09（70 字超）/ W10（半数超の枚）、design-principles、SKILL.md 手順 7
- 2026-09-19 | 図だけで伝わる枚にもリード文を機械的に付けていた | リード文は任意。図・画面で伝わるなら省く。lint は warn でなく info に | lint I03（旧 W01）、design-principles、slide-plan
- 2026-09-19 | ユーザーの下書きを渡されて、合意済みの構成を丸ごと寄せてしまった。左右 3 対 3 に揃えるため実在しない打ち手（検索改善）を足した | 他人の資料からは要素を借り、構造は借りない。対称のために項目を作らない | SKILL.md 手順 8、slide-plan、design-principles
- 2026-09-19 | 「中身は地味なデータ整備」と書いた。聴衆にその仕事をしている人がいる | 他者の取り組みに評価語を付けない。事実と条件だけ | design-principles、slide-plan
- 2026-09-19 | 表紙の名前・イベント名、章扉の「01」を既定で入れていた | どれも任意。ユーザーの指定がなければ最小にし、聞かれたら足す | SKILL.md 既定値
- 2026-09-19 | 「ありがとうございました」の締めスライドは要らない。企業を並べる枚にはロゴが欲しい。キャプションの改行位置を画像として確かめていない | 締めは外部登壇のみ。企業カードには `.card-head` + `.icon.brand` にロゴ（simple-icons に無い企業は公式 favicon 128px）。figcaption は 1 行に収めるか `<br>` で語の途中で折れないようにする | story-structures、layout-patterns、テーマ `.card-head`、qa-checklist

## 進め方

- 2026-09-19 | スライドを作るたびに `~/develop` 直下が散らかる | 置き場所（ワークスペース）を初回に 1 度だけ聞いて `~/.config/tech-slides/config.json` に保存し、以後はその下にデッキごとの `<slug>/` を切る。カレントディレクトリに作業ファイルを置かない | `scripts/workspace.sh`、両 SKILL.md の冒頭、`plan-template.md` の出力先

- 2026-09-19 | 別の雰囲気のデザインも見たい。「〇〇みたいな」で指示したい | テーマを 5 つ用意し、Markdown を変えずに `theme:` だけで差し替える。固有名・雰囲気語 → テーマの対応表を持ち、選んだ理由を一文添えて確認する。構想フェーズで見た目は 1 問だけ聞く | `themes/*.css`、`style-catalog.md`、両 SKILL.md、`plan-template.md`

- 2026-09-19 | 構想と作成を分けたい。構想の成果物は Markdown 1 つ | `slide-plan`（構想）→ `<slug>.plan.md` → `tech-slides`（作成）の 2 スキル構成。構成が無いのに作成に入らない | SKILL.md 両方
- 2026-09-19 | 構想フェーズで言いなりにならない | 聴衆の立場で代案と理由を出す。決めるのはユーザー | `slide-plan/SKILL.md` の「姿勢」
- 2026-09-19 | 画像（アプリの画面・ツールのロゴ・記事や動画のサムネイル）を考慮していない | 画像が根拠になる枚を構想フェーズで洗い出し、取れるものは取り、取れないものは条件付きで依頼する。文字だけで済ませない | `assets.md`、`icon.mjs --brand`、`fetch_asset.sh`、`screenshot.sh`
- 2026-09-19 | 見本の崩れ（ベン図・折り返し・境界）に自分で気づけていなかった | コンタクトシートだけで済ませず、**全ページを 1 枚ずつ原寸で見る**。さらに作った本人は見落とすので、**別のエージェント（slide-review）に画像を渡して指摘させる** | `tech-slides/SKILL.md` Phase 4、`qa-checklist.md`

## 未対応・保留

- 編集可能な PPTX / Google Slides 出力（2026-09-19：PDF と HTML で足りるとの判断。需要が出たら pptxgenjs の第二レンダラーを検討）
- ツリー図の専用 class（Mermaid で代替中）
